Let me first check the relevant skills, then build this.Now let me build the full GitHub Action as an OSS project.Now let me verify everything looks right:Complete OSS GitHub Action, ready to publish. Here's the structure:

```
autodoc-action/
├── action.yml              ← GitHub Action definition
├── src/autodoc.py          ← Core engine (repo scanner + Claude API)
├── examples/.github/workflows/autodoc.yml  ← Copy-paste workflow
├── README.md               ← Bilingual EN/JA with quick start
├── LICENSE                 ← MIT
└── .gitignore
```

**What it does on every `git push` to main:**
1. Scans repo structure, source files, config files, git history
2. Calls Claude to generate 4 docs: `ARCHITECTURE.md`, `API.md`, `ONBOARDING.md`, `DECISIONS.md`
3. Commits them back to `docs/autodoc/`
4. Infinite loop prevention via `paths-ignore`

**To ship this:**
1. Create `kiara-inc/autodoc-action` repo on GitHub
2. Push these files
3. Tag `v1` release
4. Users just need the 10-line workflow YAML + an API key secret

**Next steps to consider:**
- Add **MCP Server** layer (Phase 2) for real-time prompt capture
- Add **cost dashboard** showing API spend per run
- Add **diff mode** — only regenerate docs for changed files (cheaper)
- Marketplace listing for GitHub Actions marketplace visibility
